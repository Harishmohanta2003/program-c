#include <stdio.h>

int main(){
    int x, sum =0, rem;
    printf("enter a number:");
    scanf("%d",&x);
    do{
        rem=a%10;
        sum=sum+rem;
        a=a/10;
    } while(a!=0);
    printf("sum of digits:%d\x",sum);
    
    return 0;
}
